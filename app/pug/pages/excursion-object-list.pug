extends ../base/layout

block variables
  - var title = 'Экскурсионные объекты'
  - var folder = 'new-year'

block header
  include ../blocks/header
block main
  main.grid
    .container
      //breadcrumbs
      include ../elements/breadcrumbs
      section.top-about
        h1 #{title}
      .tabs-row
        a.tab-list.tab-list--ex(href="/excursion-list.html")
          img(src="dist/img/services/1.png", alt="Экскурсии")
          span Экскурсии
        a.tab-list.tab-list--active.tab-list--ex-object(href="javascript:void(0)")
          img(src="dist/img/services/2.png", alt="Эксурсионные объекты")
          span Эксурсионные объекты
    //finder
    include ../blocks/finder-horisontal
    // single
    include ../blocks/sliders/single-tour
    // form
    include ../blocks/form-footer
  // footer
  include ../blocks/footer

block scripts
  include ../base/scripts